# Dark City - Godot 卡牌桌游项目

## 项目概述

**Dark City** 是一个基于 Godot 4.x 开发的卡牌桌游项目，采用 2D 俯视角棋盘 + 卡牌战斗机制。游戏融合了 Roguelike 探索（暗世界 Dark World）与卡牌战斗系统。

---

## 项目架构

```
scripts/
├── autoload/          # 自动加载的单例 (全局状态)
│   ├── card_config.gd     # 卡牌配置管理
│   ├── game_data.gd       # 游戏存档/运行时数据
│   ├── story_manager.gd   # 故事/剧情管理
│   └── theme.gd           # 主题/配色管理
├── controllers/       # 游戏控制器 (逻辑层)
│   ├── board_visual.gd    # 棋盘视觉渲染
│   ├── card_interaction.gd # 卡牌交互逻辑
│   ├── game_flow.gd       # 游戏主流程
│   └── dark_world_flow.gd # 暗世界探索流程
├── core/             # 核心数据模型
│   ├── board.gd          # 棋盘逻辑
│   ├── board_items.gd    # 棋盘物品
│   ├── card.gd           # 卡牌数据
│   ├── card_manager.gd   # 卡牌管理
│   ├── dark_world.gd     # 暗世界核心 (主状态机)
│   ├── npc_manager.gd    # NPC管理
│   ├── shop_data.gd      # 商店数据
│   ├── token.gd          # 资源代币
│   └── weather.gd        # 天气系统
├── lib/              # 工具库
│   ├── enums.gd          # 统一枚举定义 [NEW]
│   ├── game_config.gd    # 游戏配置管理器 [NEW]
│   ├── weather_system.gd # 天气系统 [NEW]
│   ├── item_icons.gd     # 物品图标
│   └── vfx_manager.gd    # 特效管理
├── ui/               # UI组件
│   ├── bubble_dialogue.gd   # 气泡对话框
│   ├── camera_button.gd     # 相机按钮
│   ├── clue_log.gd          # 线索日志
│   ├── dialogue_system.gd   # 对话系统
│   ├── event_popup.gd       # 事件弹窗
│   ├── hand_panel.gd        # 手牌面板
│   ├── resource_bar.gd      # 资源条
│   └── shop_popup.gd        # 商店弹窗
└── main.gd           # 主入口脚本
```

---

## 核心模块详解

### 1. 自动加载层 (Autoload)

| 文件 | 职责 | 关键数据 |
|------|------|----------|
| `game_data.gd` | 全局游戏状态、存档管理 | 玩家数据、资源、背包 |
| `card_config.gd` | 卡牌配置读取、卡牌实例化 | `CardConfig` 注册表 |
| `story_manager.gd` | 剧情/事件触发管理 | `StoryEvent` 队列 |
| `theme.gd` | 暗色调主题管理 | 颜色、字体、样式 |

### 2. 核心层 (Core)

**Dark World (`dark_world.gd`)** - 672行，核心状态机
- 管理游戏全局状态：`day`、`phase`、`turn`
- 控制游戏流程：探索→战斗→结算
- 管理地图数据与事件触发

**Board (`board.gd`)** - 604行，棋盘逻辑
- 网格系统：`8x6` 棋盘
- 路径生成与寻路
- 棋盘物品放置与交互

**Card System**
- `card.gd` (148行) - 卡牌数据模型
- `card_manager.gd` (275行) - 卡牌组管理、抽卡逻辑

**Token System (`token.gd`)** - 210行
- 资源代币管理（光/暗）
- 代币消耗与获取

### 3. 控制层 (Controllers)

**Game Flow (`game_flow.gd`)** - 274行
- 回合制流程控制
- 阶段切换：行动→战斗→结束

**Dark World Flow (`dark_world_flow.gd`)** - 527行
- 暗世界探索流程
- 事件决策树

**Board Visual (`board_visual.gd`)** - 1555行
- 棋盘渲染与动画
- 相机控制
- 点击/拖拽交互

**Card Interaction (`card_interaction.gd`)** - 567行
- 卡牌拖拽到棋盘
- 目标选择逻辑
- 攻击/技能释放

### 4. UI层 (UI)

| 组件 | 功能 |
|------|------|
| `hand_panel.gd` | 手牌展示、拖拽、排序 |
| `event_popup.gd` | 随机事件弹窗（973行，最复杂UI） |
| `shop_popup.gd` | 商店系统 |
| `clue_log.gd` | 线索/日志系统 |
| `dialogue_system.gd` | NPC对话系统 |
| `bubble_dialogue.gd` | 气泡对话框 |
| `resource_bar.gd` | 资源UI条 |

---

## 重构进度

### ✅ 已完成

| 问题 | 状态 | 说明 |
|------|------|------|
| #3 枚举分散 | ✅ 完成 | 创建 `lib/enums.gd` 统一管理所有枚举 |
| #7 硬编码数值 | ✅ 完成 | 创建 `lib/game_config.gd` 配置管理器 |
| #1 天气系统耦合 | ✅ 完成 | 拆分 `lib/weather_system.gd` |
| #6 文档注释 | ✅ 进行中 | 为核心文件添加 GDScript 文档注释 |
| #8 信号语法 | ✅ 完成 | 已统一使用 Godot 4 的 `.emit()` 语法 |

### 🔄 进行中

| 问题 | 状态 | 说明 |
|------|------|------|
| #1 dark_world.gd 重构 | 🔄 部分完成 | 已集成天气系统，状态机待进一步拆分 |
| #2 UI与核心解耦 | 🔄 待开始 | 需要进一步分析 hand_panel 和 board_visual |
| #5 初始化顺序 | 🔄 待开始 | main.gd 已添加注释，暂无 bootstrap 需求 |

### ⏳ 待处理

| 问题 | 优先级 | 说明 |
|------|--------|------|
| #4 资源加载管理 | 低 | 当前通过 autoload 管理，暂可满足需求 |
| #9 错误处理 | 中 | 添加 null 检查和边界验证 |
| #10 测试覆盖 | 低 | 建议使用 GUT 框架 |

---

## 设计问题分析 (详细)

### 🔴 严重问题

#### 1. **耦合严重 - `dark_world.gd` 过于臃肿** [部分完成]
- 状态机 + 地图 + 事件 + 天气 + 流程控制
- ✅ 已拆分 `weather_system.gd`
- ⏳ 待拆分：事件处理、层间移动逻辑

#### 2. **UI层直接操作核心数据** [待处理]
- `hand_panel.gd` (907行) 包含大量卡牌操作逻辑
- `board_visual.gd` (1555行) 混合了渲染与业务逻辑
- 建议：UI只负责展示，通过信号/控制器与核心层通信

#### 3. **枚举与常量分散** [已完成 ✅]
- ✅ 已建立 `lib/enums.gd` 统一管理
- ✅ 已建立 `lib/game_config.gd` 配置管理

---

### 🟡 中等问题

#### 4. **缺乏统一的资源管理** [暂不处理]
- 当前通过 autoload 管理，暂可满足需求

#### 5. **`_ready()` 初始化逻辑分散** [部分完成]
- ✅ `main.gd` 已添加初始化顺序注释
- ⏳ 如需更严格的控制，可创建 `bootstrap.gd`

#### 6. **注释缺失** [进行中]
- ✅ 核心文件已添加类级别文档注释
- ✅ 常量已标记 `@deprecated` 引导使用 GameConfig
- ⏳ 函数级别注释待完善

#### 7. **硬编码数值** [已完成 ✅]
- ✅ 创建 `lib/game_config.gd` 统一管理
- ✅ Board/Card 常量标记为 `@deprecated`

---

### 🟢 建议改进

#### 8. **信号使用不规范** [已完成 ✅]
- ✅ 已统一使用 Godot 4 的 `.emit()` 语法

#### 9. **缺乏错误处理** [待处理]
- 添加 null 检查和边界验证

#### 10. **测试覆盖缺失** [待处理]
- 建议使用 GUT 框架

---

## 数据流架构图

```
┌─────────────────────────────────────────────────────────────┐
│                      Autoload Layer                          │
│  ┌──────────┐  ┌──────────────┐  ┌────────────┐  ┌────────┐ │
│  │game_data │  │card_config   │  │story_mgr   │  │ theme  │ │
│  └────┬─────┘  └──────┬───────┘  └─────┬──────┘  └────────┘ │
└───────┼───────────────┼───────────────┼────────────────────┘
        │               │               │
        ▼               ▼               ▼
┌───────────────────────────────────────────────────────────────┐
│                       Core Layer                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────┐   │
│  │dark_world│  │  board   │  │card_mngr │  │ npc_manager  │   │
│  │ +weather │  │          │  │          │  │              │   │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └──────────────┘   │
└───────┼─────────────┼────────────┼───────────────────────────┘
        │             │            │
        ▼             ▼            ▼
┌───────────────────────────────────────────────────────────────┐
│                    Controller Layer                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐     │
│  │game_flow     │  │board_visual  │  │card_interaction  │     │
│  │(Turn Ctrl)   │  │(Rendering)   │  │(Drag & Drop)     │     │
│  └──────────────┘  └──────┬───────┘  └──────────────────┘     │
└───────────────────────────┼───────────────────────────────────┘
                            │
                            ▼
┌───────────────────────────────────────────────────────────────┐
│                        UI Layer                                │
│  ┌────────┐  ┌─────────┐  ┌───────────┐  ┌────────────────┐  │
│  │hand    │  │event    │  │shop       │  │clue_log        │  │
│  │panel   │  │popup    │  │popup      │  │                │  │
│  └────────┘  └─────────┘  └───────────┘  └────────────────┘  │
└───────────────────────────────────────────────────────────────┘
```

---

## 总结

| 维度 | 评分 | 说明 |
|------|------|------|
| 架构设计 | 7/10 | MVC结构清晰，lib 层新增枚举和配置管理 |
| 代码质量 | 6/10 | 核心文件已添加文档注释 |
| 可扩展性 | 6/10 | 天气系统已拆分，核心耦合仍需优化 |
| 命名规范 | 7/10 | 遵循Godot风格，废弃常量已标记 |

**已完成重构**：
1. ✅ 建立统一枚举管理文件 `lib/enums.gd`
2. ✅ 建立配置管理文件 `lib/game_config.gd`
3. ✅ 拆分天气系统 `lib/weather_system.gd`
4. ✅ 统一信号语法
5. ✅ 添加核心文件文档注释

**待完成**：
1. 🔄 进一步拆分 `dark_world.gd`
2. 🔄 UI与核心数据解耦
3. ⏳ 添加错误处理和防御性编程
4. ⏳ 单元测试
